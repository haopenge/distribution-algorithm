

dubbo:
  scan:
    base-packages: com.uu.service
  protocol:
    name: dubbo
  registry:
    address: zookeeper://localhost:2181
    group: dubbo
  metadata-report:
    address: zookeeper://localhost:2181
    group: dubbo
  consumer:
    check: false
spring:
  application:
    name: dubbo-test
